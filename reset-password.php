<?php
require __DIR__.'/auth.php';require __DIR__.'/includes/foundation/recovery.php';header('Referrer-Policy: no-referrer');$done=false;$error='';$token=(string)($_POST['token']??$_GET['token']??'');
if($_SERVER['REQUEST_METHOD']==='POST'){csrf();try{if(($_POST['password']??'')!==($_POST['confirm_password']??''))throw new InvalidArgumentException('Passwords must match.');apply_recovery(db(),$token,(string)($_POST['password']??''));$_SESSION=[];session_regenerate_id(true);$done=true;}catch(InvalidArgumentException $e){$error=$e->getMessage();}catch(Throwable $e){$error='Password reset is unavailable. Please contact your administrator.';}}
?><!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="referrer" content="no-referrer"><title>New password · sHRMS</title><link rel="stylesheet" href="assets/foundation.css"></head><body class="auth-screen"><main class="auth-shell card"><span class="eyebrow">ARKENTECH · sHRMS</span><h1><?= $done?'Password updated':'Choose a new password'?></h1><?php if($done):?><p>All existing sessions have been signed out.</p><a class="button primary" href="login.php">Sign in</a><?php else:if($error):?><div class="alert error" role="alert"><?=htmlspecialchars($error)?></div><?php endif?><form method="post"><input type="hidden" name="csrf" value="<?=htmlspecialchars($_SESSION['csrf'])?>"><input type="hidden" name="token" value="<?=htmlspecialchars($token)?>"><label>New password<input type="password" name="password" minlength="12" maxlength="72" autocomplete="new-password" required></label><label>Confirm password<input type="password" name="confirm_password" minlength="12" maxlength="72" autocomplete="new-password" required></label><button class="button primary">Save password</button></form><?php endif?></main></body></html>
